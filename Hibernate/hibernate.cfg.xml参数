<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
          "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
          "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<!-- Generated by MyEclipse Hibernate Tools.                   -->
<hibernate-configuration>

    <session-factory>
   		<!--
   		dialect 告诉hibernate连接的数据库是MySQL
   		 -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>
        <property name="connection.url">jdbc:mysql:///db</property>
        <property name="connection.username">root</property>
        <property name="connection.password">123</property>
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="myeclipse.connection.profile">mysql</property>
    
        	    <property name="show_sql">true</property>	
    	<!-- 
    			ddl:数据定义语言 create alter drop
    			dml:数据操纵语言 update insert delete
    			dcl：数据控制语言  grant revoke
    			dql:数据查询语言 select
    	hbm的含义是自配置文件
    	2的含义是to
    	ddl数据定义语言
    	hbm2ddl  根据hbm映射文件自动创建表
    	 -->
    	 
    	 <property name="hbm2ddl.auto">update</property>
    	 <!-- 四种取值：
    	 	create 每次创建sessionFactory都会删除旧表 创建新表 
    	 	update 表不存在就创建，存在就更新
    	 	create-drop 每次创建sessionFactory都会删除旧表 创建新表 
    	 	除此之外 关闭sessionFactory也会删除表
    	 	validate 永远不会删除 也不会创建表 
    	 	仅仅是检查一下hbm映射文件描述的表结构与数据库的表结构是否一致
    	 	一致就什么也不做 不一致就报错
    	  -->
    	<mapping resource="west/b_utils/User.hbm.xml"></mapping>
    
    
    </session-factory>

</hibernate-configuration>
