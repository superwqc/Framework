hibernate二级缓存

1.缓存的作用：提高读写速度。
2.一级缓存存活时间过短（一个session会话），对系统的性能提升没有太大帮助，一级缓存主要是为了让session管理持久化对象。
3.为了提高系统性能，就应该使用二级缓存，因为二级缓存的生命周期，与整个web应用一样。
4.但二级缓存并不是越多越好，因为二级缓存以后，以后每次对象的修改，不但要修改数据库，还要额外同步更新二级缓存中的数据，造成了额外的开销。
5.所以对于经常查询，很少修改的类，可以使用二级缓存；针对于经常需要修改很少查询的类，避免使用二级缓存。